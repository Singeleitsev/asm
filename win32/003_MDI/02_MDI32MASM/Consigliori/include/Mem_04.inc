;--------------------------------------------------------------------------------------
;ћакрос, загружающий значение из одной переменной в пам€ти в другую переменную в пам€ти
;”слови€: оба значени€ должны быть 64-битными, регистр rax разрушаетс€
;¬ход: им€ переменной-приЄмника, им€ переменной-источника
;--------------------------------------------------------------------------------------
m64m64 MACRO var1,var2
	mov rax,var2
	mov var1,rax
ENDM ;m64m64

;--------------------------------------------------------------------------------------
;ћакрос, загружающий значение из одной переменной в пам€ти в другую переменную в пам€ти
;”слови€: оба значени€ должны быть 32-битными, регистр eax разрушаетс€
;¬ход: им€ переменной-приЄмника, им€ переменной-источника
;--------------------------------------------------------------------------------------
m32m32 MACRO var1,var2
	mov eax,var2
	mov var1,eax
ENDM ;m32m32

;--------------------------------------------------------------------------------------
;ћакрос, загружающий адрес одной переменной в пам€ти в другую переменную в пам€ти
;”слови€: –азмер переменной-приЄмника должен быть 64-битным, регистр rax разрушаетс€
;¬ход: им€ переменной-приЄмника, им€ переменной-источника
;--------------------------------------------------------------------------------------
m64addr MACRO var1,var2
	lea rax,var2
	mov var1,rax
ENDM ;m64addr

;--------------------------------------------------------------------------------------
;ћакрос, загружающий адрес одной переменной в пам€ти в другую переменную в пам€ти
;”слови€: –азмер переменной-приЄмника должен быть 32-битным, регистр eax разрушаетс€
;¬ход: им€ переменной-приЄмника, им€ переменной-источника
;--------------------------------------------------------------------------------------
m32addr MACRO var1,var2
	lea eax,var2
	mov var1,eax
ENDM ;m32addr